# Claude 작업 로그 — 위빠사나 실습 기능

최종 업데이트: 2026-08-03

## 담당 목표

기존 아나빠나사띠 16단계 학습·호흡 기능은 그대로 보존하고, 그와 별개의 **위빠사나 호흡·몸·감각 관찰 실습법**을 추가한다. 기존 기능을 위빠사나로 이름만 바꾸거나 덮어쓰지 않는다.

## 권장 구현 경계

- 새 콘텐츠 데이터: `src/data/vipassana.js` 또는 `src/data/vipassana/`
- 새 실습 페이지: `src/pages/Vipassana.jsx` 및 필요 시 하위 컴포넌트
- 새 전용 스타일: `src/pages/Vipassana.css`
- 기존 `src/data/stages.js`의 아나빠나사띠 16단계는 보존
- 결제·인증·Supabase 함수는 수정하지 않음

라우팅·Navbar·홈 화면 연결을 위해 공용 파일을 수정해야 하면 먼저 이 로그에 변경 목적과 파일을 `IN_PROGRESS`로 기록하고, 기존 Codex 변경을 병합한다.

## 콘텐츠 방향

- 위빠사나를 단순한 호흡 조절법으로 오해하지 않도록, 자연스러운 호흡과 몸·감각·마음 현상의 관찰을 중심으로 구성한다.
- 의료 효과를 단정하지 않고, 불편감이 심하면 중단하도록 안전 안내를 포함한다.
- 아나빠나사띠와 위빠사나의 관계·차이를 입문자가 이해할 수 있게 설명한다.
- 짧은 입문 실습부터 단계적으로 확장할 수 있게 설계한다.

## 시작 전 확인

1. `HANDOFF.md`와 `docs/HANDOFF_CODEX.md` 읽기
2. `git status` 확인
3. 기존 아나빠나사띠 화면·데이터 구조 확인
4. 새 파일 중심의 구현 계획을 이 로그에 `IN_PROGRESS`로 기록
5. 공용 파일 충돌 여부 확인 후 구현

## 현재 상태

- 담당 범위 확장: **요가 호흡법(프라나야마)을 먼저 추가**, 위빠사나는 그 다음. (사용자 지시 2026-08-03)
- `요가 호흡법`: **READY_FOR_DEPLOY** — ✅데이터 ✅페이지 ✅타이머 ✅사운드·색감·요가디자인 고도화 완료(미배포). 기획: `docs/PRANAYAMA_PLAN.md`.
  - 장르 차별화: `.yoga-theme`(따뜻한 테라코타) + 연꽃 만다라 배경 + 오브 색 그라데이션 + 숨결 사운드(`yogaSound.js`). 아나빠나사띠(세이지)와 대비.
  - 3단계: `YogaPractice.jsx`를 실제 타이머로 교체 — paced(복식/박스/웃자이/브라마리/시탈리/완전요가), alternate(나디쇼다나 좌우 콧구멍 안내), pulsed(카팔라바티/바스트리카 라운드·펄스 + 금기 동의 모달). Yoga.css에 오브/실습 스타일 추가.
  - dev 검증: 세 모드 모두 실동작 확인(복식=들이쉬기 카운트, 나디쇼다나=왼쪽콧구멍 안내, 카팔라바티=1라운드 16/20회), 콘솔 에러 0.
  - 데이터: `src/data/ashtanga.js`(8단계), `src/data/pranayama.js`(개념+5단계+9기법+타이머설정).
  - 페이지(신규): `src/pages/YogaBreathing.jsx`(허브), `YogaAshtanga.jsx`(8단계), `YogaTechnique.jsx`(기법 상세), `YogaPractice.jsx`(**임시 placeholder** — 3단계에서 실제 타이머로 교체), `Yoga.css`.
  - ⚠️ **공용 파일 병합함(추가만)**: `src/App.jsx`(라우트 `/yoga`,`/yoga/ashtanga`,`/yoga/:id`,`/yoga/:id/practice` 추가), `src/components/Navbar.jsx`(“요가 호흡” 링크 추가). Codex 변경 덮어쓰지 않음.
  - 검증: dev에서 /yoga(5단계·9기법·⚠️금기표시), /yoga/ashtanga(8단계·야마니야마 세부) 렌더 확인, 콘솔 에러 0. `YogaPractice`는 placeholder.

### 2026-08-04 — PLAN (아키텍처: 4개 앱 분리 + 재사용 스킬)
- 요청: 하나의 코드에서 통합앱 + 아나빠나사띠·요가·위빠사나 단독앱 4개를 뽑을 수 있게 폴더 모듈화. 결제·소셜로그인 등을 "스킬"로 재사용(토큰 절약).
- 설계: `docs/ARCHITECTURE.md` — features/skills/ui/apps 구조, 스킬 계약(auth/payment/premium/journal/breath-engine), 4앱 구성, 빌드전략, **점진 이관 순서**(big-bang 금지).
- 소유: Claude=features·breath-engine·journal·ui / Codex=auth·payment·premium·배포.
- 남은 일: 합의 후 (2)홈·네비 재편+아나빠나사띠 허브화 → (3)features/ 이동 → (4)breath-engine 추출. skills/auth·payment는 Codex와 합의.

### 2026-08-04 — READY_FOR_DEPLOY (요가 사운드·색감·디자인 고도화)
- 요청: 요가 실습에 어울리는 사운드, 오브 색 그라데이션 전환, **장르별 색/디자인 차별화**(아나빠나사띠 vs 요가), 요가풍 배경/오브제.
- 구현:
  - `src/lib/yogaSound.js`(신규): 따뜻한 "숨결 패드음"(들숨 차오름/날숨 잦아듦/멈춤 은은) + 펄스용 짧은 톤. bowl.js AudioContext 공유(수정 안 함).
  - `src/pages/YogaPractice.jsx`: 페이즈 색(들숨 앰버 #e0a15c / 멈춤 로즈 #d38aa0 / 날숨 인디고 #7f86c2) 그라데이션 전환, 전체화면 숨결 배경(`.yoga-breath-bg`), 연꽃 만다라 회전 모티프(인라인 SVG), 중앙 물결 리플, 숨결 사운드 토글.
  - `src/pages/Yoga.css`: **`.yoga-theme` 지역 변수 재정의**(따뜻한 테라코타 --sage/--sage-deep/배경) → 요가 전 페이지가 아나빠나사띠(세이지)와 다른 장르 색감. 오브/배경/리플/만다라/사운드토글 스타일 추가.
  - `YogaBreathing/YogaAshtanga/YogaTechnique/YogaPractice` 루트에 `yoga-theme` 클래스 적용.
- 검증: dev에서 오브 색전환·만다라·리플·배경·라벨 확인, 콘솔 에러 0. 미배포.
- Codex와 겹치는 파일: 없음(모두 요가 전용 파일). 결제·인증·아나빠나사띠 미변경.
- 남은 일: (선택) 위빠사나 / 요가 실습 수행일지 기록. 배포는 Codex 또는 사용자 요청 시.

### 2026-08-03 — READY_FOR_DEPLOY (요가 3단계 타이머)
- 요청: 요가 3단계(실제 호흡 타이머) 구현 + 웹 확인.
- 구현: `src/pages/YogaPractice.jsx` placeholder → 실제 타이머. 3모드:
  - paced: `timer.phases` 1초 순환, 오브 확대/축소, 단계 라벨·cue, 수행시간 선택(2/3/5분).
  - alternate(나디쇼다나): `timer.steps` 순환, 콧구멍(왼/오/양쪽) 안내 표시.
  - pulsed(카팔라바티/바스트리카): 금기 동의 모달 → 라운드×펄스(setTimeout 재귀), 라운드 사이 휴식 카운트, 완료 처리.
- 수정 파일: `src/pages/YogaPractice.jsx`(교체), `src/pages/Yoga.css`(오브/실습 스타일 추가). 공용 파일 추가 없음(라우트는 2단계에서 이미 추가됨).
- 검증: dev에서 3모드 실동작 확인, 콘솔 에러 0. 미배포.
- Codex와 겹치는 파일: 없음(이번 단계).
- 남은 일: (선택) 실습 완료 기록을 수행일지에 남기기, 사운드/나레이션, 위빠사나. 배포는 Codex 또는 사용자 요청 시.

### 2026-08-03 — READY_FOR_REVIEW (요가 2단계 페이지)
- 요청: 요가 호흡법 2단계(페이지) 구현 + 웹 확인.
- 결정/구현: 허브(아쉬탕가 개요→프라나야마 개념(4요소·아나빠나사띠 대비)→5단계 기법 목록), 아쉬탕가 8단계 학습, 기법 상세(효과·방법·비율·주의·금기경고+실습버튼). 초보자 친화 카피.
- 수정 파일: 신규 4페이지+Yoga.css / 공용 App.jsx·Navbar.jsx(추가 병합).
- 검증: dev 렌더 확인, node/브라우저 콘솔 에러 0.
- Codex와 겹치는 파일: App.jsx, Navbar.jsx (추가만, 결제/인증 코드 미변경).
- 남은 일: 3단계 실제 호흡 타이머(YogaPractice 교체). 배포는 Codex 또는 사용자 요청 시.
- `위빠사나`: `PLANNED`(기획 전).
- Codex의 결제·인증·배포 변경(작업 트리 미커밋)은 보존해야 함. reset/revert 금지.

## 작업 기록

### 2026-08-19 — DONE (3일 체험 · 쿠폰 · 사업자 정보 · 결제 점검)

**사업자 정보 확정** (등록증·신고증 실물 확인)
- 상호가 **소울메이트**다. 코드·문서 전부 "소울매트"로 잘못 적혀 있었다.
  법정 표시는 등록증 상호를 그대로 써야 한다(전자상거래법 제10조 1항 1호).
- 사업자등록번호 824-08-01492 / 통신판매업 2020-서울강서-2643 / 0507-1316-1623
- 업종에 이미 `소프트웨어 개발 및 공급업`, `전자상거래 소매업`이 있다. 업종 추가 불필요.
- **일반과세자** → 소비자 표시가는 부가세 포함 총액. 유료약관 제1조에 명시.
- ⚠️ **강서구청 통신판매업 소재지 변경신고가 안 돼 있다.**
  신고증 주소(공항대로 325)는 개업 초기 주소이고 양천로 564로 이전했다(대표 확인).
  세무서 신고만 하고 구청은 빠졌다. 미신고는 과태료 대상. 실결제 전에 정리할 것.
  같은 변경신고에서 **인터넷 도메인**에 이 사이트를 넣으면 한 번에 끝난다.

**3일 무료 체험 — 카드를 받지 않는다**
- 가입하면 3일 열리고 끝나면 잠긴다. 결제수단을 안 받으니 자동 전환이 없다.
- 그래서 **2025.2 시행 구독 규제가 애초에 걸리지 않는다**(갱신 전 고지·해지 실동작·
  다크패턴 6종). 개발도 분쟁도 함께 사라진다. 반응을 본 뒤 빌링키를 붙이는 게 순서다.
- 3일로는 AI 코칭·장기 분석의 가치를 못 느낀다(데이터가 없다). 안내는 소리·패턴 중심으로.

**쿠폰**
- 종류별 공용 코드를 쓰지 않는다. 한 명이 캡처해 올리면 끝난다.
  요청 한 건마다 1인용 코드를 발행한다(`issue_coupon`).
- 코드에서 `0 O 1 I L`을 뺐다. 메일로 받아 옮겨 적을 때 반드시 틀린다.
- 같은 주소·같은 종류로 다시 부르면 **새로 만들지 않고 기존 코드**를 돌려준다.
  요청 메일이 두 번 와도 두 장이 나가지 않는다.
- 요가 강사 3개월 무료. 자격증 확인 문턱을 없애고 기간을 넉넉히 줘서 수업에 써보게 한다.
- 발송은 자동화하지 않았다. 메일 본문은 신뢰할 수 없는 입력이고(지시가 담겨 올 수 있다),
  잘못 나간 코드는 회수할 수 없다. → `docs/COUPON_OPS.md`

**🔴 고친 실제 결함 4가지**
1. `PremiumContext`가 localStorage 값을 OR로 인정했다. **개발자도구에 값 하나만 넣으면
   프리미엄이 열렸다.** 실결제를 여는 이상 그대로 둘 수 없어 걷어냈다.
2. `confirm-payment`가 **배포본 v1 그대로**였다. 3개월 요금제를 추가하며 로컬만 고치고
   배포를 빠뜨려, quarterly 결제가 서버 금액 검증에서 거부될 상태였다.
3. 같은 함수가 기간을 **지금부터 다시 셌다.** 체험·쿠폰이 남은 사람이 결제하면 남은 날을
   뺏겼다. 돈 낸 사람이 손해 보는 구조다. 이어 붙이도록 고쳤다(v3 배포).
4. 화면이 **없는 기능을 팔고 있었다.** "7일 무료 체험", "자동 갱신" 둘 다 코드에 없었다.
   테스트 키라 티가 안 났을 뿐, 라이브로 바꾸면 체험 버튼을 누른 사람에게 즉시 청구된다.
   또 "광고 없는 공간"을 혜택으로 적었는데 원래 광고가 없다.

**npm run check:launch** — 실결제 전 점검. 막는 항목이 있으면 종료 코드 1.
- 처음엔 화면 문구를 정규식으로 훑었는데 "자동으로 다시 결제되지 **않습니다**"를 약속으로
  읽었다. 부정문은 정규식으로 가릴 수 없다. `payments.js`가 `BILLING_MODEL`을 선언하고
  그 선언만 검사하도록 바꿨다.

**검증** — DB 함수는 트랜잭션 안에서 돌리고 롤백했다(쿠폰 사용 9항목·발행 8항목 전부 통과).
375px 가로 넘침 없음, 본문·입력칸 16px, 푸터 사업자 정보 정상 표기.

**⚠️ 커밋 23개가 로컬에만 있다. 배포된 사이트는 아직 예전 상태다.**

### 2026-08-20 — DONE (법정 고지 문서 3종 · Edge Function v2)

**법정 고지 문서** — `src/data/legal.js`, `pages/Legal.jsx`, `components/SiteFooter.jsx`
- 국내 사례 조사(마보·코끼리·밀리의서재·메일리) 후 **베끼지 않고** 우리 구조로 작성.
  ⚠️ 마보·코끼리는 앱마켓 인앱결제라 "환불은 앱마켓 정책에 따름"으로 책임을 넘긴다.
  우리는 토스 자체 결제라 그걸 복사하면 환불 규정이 통째로 빈다.
- 구조: 메일리식 분리(약관 / 유료·정기결제 약관 / 처리방침) + 마보식 처리방침 설계
- **환불(사용자 결정): 위약금 없음.** 7일 이내 미이용 100% / 그 외 이용일수만 공제 후 전액
- **오픈기념가 평생 유지(사용자 결정)** — 가격을 안 올리니 전자상거래법 제13조 6항
  "대금 증액 30일 이내 별도 동의" 절차 구현이 **불필요해졌다**. 개발 부담이 크게 줄었다
- 우리 고유: 건강 고지(의료행위 아님·금기·감정 반응), 가분적 디지털콘텐츠 단서
- 국외이전 표(Supabase 뭄바이) + 근거 조항(제28조의8 1항 3호 가목) 명시
- localStorage를 자동수집장치로, 처리위탁에 Supabase·토스·GitHub 기재
- 보호책임자는 대표 본인(시행령 제32조 소상공인 자동 지정) — 주소 불필요

**⚠️ Supabase 서울 리전 이전은 보류(사용자 결정)** — 결제를 붙여 반응을 본 뒤 판단.
지금도 국외이전 표를 공개했으므로 적법하다. 나중에 옮기면 처리방침 6번 섹션만 삭제하면 된다.

**Edge Function 재배포 (중요)**
- `confirm-payment`가 **7월 배포 v1 그대로**였다. 3개월 요금제를 추가하며 로컬만 고치고
  배포를 빠뜨려, **quarterly 결제가 서버 금액 검증에서 거부될 상태**였다.
- v2 배포 완료 — PRICE에 quarterly 12900 추가, 기간 계산을 MONTHS 맵으로 일반화.
- 교훈: Edge Function은 로컬 수정이 자동 반영되지 않는다. 결제 관련 수정 시 반드시 배포 확인.

**⚠️ 실결제 전 남은 것**
- `legal.js`의 `BUSINESS`: 사업자등록번호·주소·전화·통신판매업 신고번호가 비어 있다
  (화면엔 "등록 후 표기"로 나옴). 주소는 집주소 노출 회피를 위해 비상주 사무실 검토 권장
- 토스 클라이언트 키가 **테스트 키(test_ck_)** — 실키 교체 필요
- 통신판매업 신고(정부24) — 구매안전서비스 이용확인증은 토스에서 발급
- 소감 텍스트를 민감정보로 볼지 미결(마보는 명상일기를 민감정보로 분류) —
  개인정보보호위 소상공인 무료 컨설팅 권장

- 검증: 3문서 렌더(10/9/14조), 375px 가로 넘침 없음, 표는 자기 안에서만 스크롤,
  본문 16px, 콘솔 에러 0. Edge Function v2 ACTIVE 확인.

### 2026-08-19 — DONE (MBSR 8주 커리큘럼)
> 설계 근거: `docs/ARCHITECTURE.md` — MBSR은 전통이 아니라 **프로그램**(시간축)이다.

**왜 다섯 번째 전통으로 만들지 않았나**
MBSR 핵심 실습 대부분은 빌려온 것이다 — 바디스캔은 고엔카 위빠사나, 정좌명상은 사념처,
걷기는 경행, 마음챙김 요가는 하타 요가. 전통으로 만들면 바디스캔·경행을 3중 구현하게 된다.
그래서 **시간축을 가진 커리큘럼**으로 두고 기존 실습을 `?attitude=accept`로 빌려 쓴다.

**신규**
- `src/data/mbsr.js` — 8주 커리큘럼, 7가지 마음가짐, 진도(localStorage).
  실습 참조는 `/vipassana/:id/practice?attitude=accept&program=mbsr` 링크로만 만든다.
- `src/pages/Mbsr.jsx`(개요·8주 목록·7태도) / `MbsrWeek.jsx`(주차 상세) / `Mbsr.css`
- MBSR 고유 실습 2종을 `vipassana.js`에 **level 0**으로 추가 — 건포도 한 알, 3분 호흡 공간.
  level 0은 관찰 수행 허브(1~5단계만 렌더)에 노출되지 않아 MBSR에서만 진입한다.
  이 파일이 사실상 "실습 레지스트리" 역할을 하고 있음을 주석으로 남겼다.

**기록**
- `program=mbsr`이면 `track='mbsr'`로 저장한다. 안 그러면 8주 진도가 관찰 수행에 섞인다.
- `tracks.js`에 mbsr 분기 추가 — 표시 이름도 accept 프레이밍을 따른다(몸 훑기 → 바디스캔).

**⚠️ 표기 원칙**
정식 MBSR은 인증 지도자의 8주 대면 과정이다. 개요 맨 위에 "이 앱은 **자율 실습 안내**이며
과정 수료나 자격을 대신하지 않는다"를 고지한다. 절대 "MBSR 과정"이라 부르지 않는다.

**검증(실측)**
- 1주차: 제목/마음가짐(판단하지 않기)/실습(건포도·바디스캔)/할 일 정확
- **전체 흐름**: MBSR 1주차 → 바디스캔 클릭 → `?attitude=accept&program=mbsr`로 진입 →
  화면이 "바디스캔 · 1–2주차 · 매일 45분"(MBSR 프레이밍) →
  일지 기록 "🌾 8주 마음챙김 · 바디스캔 · 1–2주차" — **설계가 코드로 완결됨을 확인**
- 진도: "이번 주 마쳤어요" → `{current:2, doneWeeks:[1]}` 저장, 자동으로 다음 주로
- 마크다운 별표(`**`)가 JSX에 그대로 찍히던 것 수정
- 375px: 8주 카드 8개·태도 7개, 가로 넘침 없음, 네비 잘림 없음, 콘솔 에러 0

**남은 것**: 6주차의 하루 종일 침묵 수련은 안내 문구로만 뒀다(앱이 대신할 수 없는 부분).

### 2026-08-19 — DONE (추이 그래프 · 자애명상 · MBSR accept 경로 · 마이크 날숨 감지)

**① 추이 그래프**
- DB 마이그레이션: `sessions.metrics jsonb` 추가(추가 전용). 실습마다 남는 숫자가 달라
  컬럼을 늘리는 대신 jsonb 하나로 받는다. 자애·MBSR도 그대로 쓸 수 있다.
- ⚠️ 브레스 카운팅 결과를 `breath_pattern` 문자열에서 파싱하지 않고 `metrics`에 따로 담았다.
  자기가 만든 문자열이라도 파싱은 문구를 바꾸는 순간 조용히 깨진다.
- `components/JournalTrend.jsx` — 이번 달 히트맵 + 카운팅 정확도 막대.
  **끊기면 0이 되는 streak은 쓰지 않는다.** 누적 히트맵이라 쌓이기만 하고 줄지 않는다.
- ⚠️ **프리미엄 경계 재조정**: 히트맵·이번 달 추이를 무료로 열었다. 리텐션 장치를
  유료화하면 습관이 안 생기고, 카운팅 정확도는 실습 결과 그 자체라 가리면 기능이 반쪽이 된다.
  프리미엄은 "더 긴 기간의 분석·검색·갈래별 비교"로 옮기고 `PRODUCT_STRATEGY.md`도 갱신.

**② 자애명상 — 네 번째 갈래**
- `src/data/metta.js` / `Metta.jsx` · `MettaDetail.jsx` · `MettaSession.jsx` · `Metta.css`
- 태도 축에서 **'계발(cultivate)'** — 조절·집중·관찰·수용 어디에도 안 속한다.
  없던 마음을 의도적으로 키우는 것이라 별도 태도다.
- engine `phrases`: 대상 × 문구 격자를 시간에 맞춰 펼친다. 대상이 바뀔 때만 종을 울린다
  (문구마다 울리면 되뇜이 끊긴다).
- 장르 색: 따뜻한 로즈 `#c08497`. 관찰이 거리를 두는 색이라면 자애는 다가가는 색.
- 경전이 자애의 첫 이익으로 "편안히 잠듦"을 꼽아 **잠들기 전 자애**를 별도 실습으로 뒀다
  — 한국 시장 최대 관문(수면)에 정통 콘텐츠로 진입하는 경로.

**③ MBSR accept 경로 — 이미 있던 키를 실제로 쓰이게**
- 확인해보니 `accept` 키는 모든 실습에 이미 있었다. 진짜 빠진 건 **그 키로 진입할 방법**이었다.
- accept에 `title`·`context`를 채웠다(같은 동작, 다른 이름). 예: 몸 훑기/신념처 → 바디스캔/1–2주차
- `getAttitude()`가 이름·맥락까지 풀어주도록 확장.
- `?attitude=accept` 쿼리로 진입 — **실측 확인**:
  `/vipassana/body-scan/practice` → "몸 훑기 · 신념처 · 바디스캔"
  `?attitude=accept` → "바디스캔 · 1–2주차 · 매일 45분"
  MBSR을 붙일 때 실습을 새로 만들 필요가 없다는 것이 코드로 증명됐다.

**④ 마이크 날숨 감지 (옵션)**
- `src/lib/breathMic.js` — `AnalyserNode`로 300Hz~4kHz 대역 에너지만 읽는다.
  **날숨만** 잡는다(들숨은 소리가 거의 없어 연구에서도 F1 0.63). ML 없이 임계값 방식.
- 첫 3초 소음 바닥 측정(중앙값 — 평균은 순간 소음에 끌려간다), 실패 시 타이머 모드로 조용히 폴백.
- `echoCancellation/noiseSuppression/autoGainControl`을 모두 끈다 — 켜두면 호흡음이 지워진다.
- 오브는 **날숨 구간에서만** 반응(`orbScale + level*0.22`). 타이머는 그대로라 감지가 실패해도 수행이 안 끊긴다.
- 프라이버시: 오디오가 기기 밖으로 나가지 않는다는 문구를 토글 바로 아래 노출.
- 한계도 그대로 안내: 조용한 코호흡은 안 잡히고 블루투스 이어폰에서 무너진다.

**검증**
- 추이 집계 로직: 앉은 날 5일 · 8월 31칸 · 앞 빈칸 6(토요일 시작) · 정확도 오래된 것부터 정렬 — 전부 통과
- 자애 타임라인: 16스텝(대상4×문구4), 대상당 75초, 종 4회(대상 전환 시에만), 흐름 확인
- MBSR accept 전환 실측 확인 / 수련 메뉴 4갈래 노출 확인
- DB: metrics 삽입을 롤백으로 검증(테스트 행 미잔류)
- 빌드 139 모듈, 새 탭 콘솔 에러 0, 배포 완료

**남은 것**: 마이크 감지의 실제 성능은 대표님이 소리 내어 날숨을 쉬어보셔야 확인됩니다
(제가 마이크에 숨을 불 수 없습니다). 자애 일지 기록도 로그인 후 확인 필요.

### 2026-08-19 — DONE (B: 브레스 카운팅 — 알아차림을 실제로 재기)
> 근거: Levinson et al. 2014 Breath Counting Task (4개 연구·400명+, 구성타당도 검증,
> 장기 수행자와 초보자를 구분). 리서치 5개 제안 중 2순위.

**문제**: 앱이 "사용자가 실제로 잘하고 있는지" 알 방법이 전혀 없었다. 일지의 집중도
점수도 본인 느낌일 뿐이다. 카메라 심박(iOS는 플래시 제어 불가)이나 마이크 호흡 감지
(정확도 78%)보다 측정 타당도가 비교가 안 되게 높고, 센서가 하나도 필요 없다.

**구현**
- `src/data/vipassana.js` — `breath-counting` 실습 추가(level 2, engine `counting`).
  호흡을 조절하지 않고 세기만 하므로 관찰 수행 트랙에 정확히 맞는다.
- `VipassanaSession.jsx` — `CountingStage` / `CountResult` 컴포넌트.
  ⚠️ **화면에 숫자를 띄우지 않는다.** 세는 일은 사용자 마음이 해야 하고,
  화면이 세어주면 측정 자체가 무의미해진다. 눌렀다는 최소 반응(0.26초 색 변화)만 준다.
- 채점: ○ 8번 + ● 1번 = 맞은 묶음 / ● 시점에 ○가 8이 아니면 틀린 묶음 /
  "다시 시작"은 **틀림이 아니라 `caught`(스스로 알아챔)로 따로 집계**한다.
  후속 연구가 오류를 둘로 나눈 것이 핵심 — 모르고 틀린 것은 주의 실패,
  스스로 잡아낸 것은 마음방황을 알아차린 것으로 의미가 정반대다.
  결과 화면에서도 `caught`만 액센트 색으로 표시한다.
- 정확도 = 맞은 묶음 / (맞은 + 틀린). `caught`는 분모에 넣지 않는다.

**일지 연동** (D 작업 덕에 바로 붙음)
- 결과 요약을 `breath_pattern`에 실어 보낸다(갈래별 자유 텍스트 필드, 요가도 같은 방식).
  DB 마이그레이션 불필요.
- `tracks.js`의 vipassana 분기에서 `breath_pattern`을 `context`보다 우선 표시하도록 수정
  → 일지에 "정확도 67% · 2/3묶음 · 스스로 알아챔 1회"가 그대로 남는다.

**검증(실측)**
- 시나리오 주입: 정상 묶음 2 + 틀린 묶음 1(○5개만) + 리셋 1
  → 정확도 67%, 맞은 2 / 모르고 틀림 1 / 스스로 알아챔 1 — **기대값과 정확히 일치**
- 기록 화면 표기: "💧 관찰 수행 · 숨을 세어보기 · 정확도 67% · 2/3묶음 · 스스로 알아챔 1회"
- 375px: 버튼 327×190 / 327×96 (눈 감고 누를 수 있는 크기), 가로 넘침 없음
- 콘솔 에러 0, 빌드 성공, 배포 완료

**남은 것**: 주차별 정확도 추이 그래프. 데이터는 쌓이기 시작했으니 일지 화면에
붙이면 된다(프리미엄 "히트맵·추세" 항목과 함께 다루는 게 자연스럽다).

### 2026-08-19 — DONE (A: 청각 페이서 + 공간 음향 + 눈감기 모드 + Wake Lock)
> 근거: 호흡명상 UX 리서치 1순위 항목.

**문제**: 명상은 눈을 감고 하는데 앱은 화면을 보라고 요구했다. `bowl.js`의 `playBowl`은
"댕—" 한 번 치는 타격음이라 페이즈 시작만 알리고 남은 시간은 알려주지 못해, 사용자가
숫자를 보려고 눈을 뜨게 된다. 모달리티 연구에서 청각이 시각보다 우수했고, 웹에서 햅틱은
iOS가 `navigator.vibrate`를 지원하지 않아 막혀 있어 청각이 유일하게 남은 최상위 채널이다.

**신규 `src/lib/breathTone.js`**
- `playBreathTone({action, durSec, baseFreq, volume, voice})` — 페이즈 전 구간을 채우는
  연속음. 들숨엔 차오르고 날숨엔 잦아든다. 음높이도 ±6%만 따라 움직인다(폭을 넓히면
  "지시"로 느껴져서 좁게 뒀다).
- `createBreathPanner()` — HRTF 패닝. 들숨에 다가오고 날숨에 멀어진다.
  `positionZ` AudioParam을 지원하면 부드럽게 움직이고, 구형 Safari는 `setPosition()`으로,
  패닝 자체가 없으면 `null`을 반환해 호출부가 조용히 건너뛴다.
- `voice`로 음색 분리: `bowl`(아나빠나사띠 비조화 배음) / `pad`(요가 따뜻한 패드).
  갈래별 사운드 정체성은 이미 사용자에게 노출된 설계라 유지했다.

**신규 `src/lib/useWakeLock.js`**
- 눈감기 모드의 **전제**. 화면을 어둡게 하면 사용자가 폰을 엎어놓거나 잠그는데,
  iOS는 화면이 잠기면 Web Audio를 즉시 죽인다.
- iOS 16.4+ / Android Chrome 지원. 미지원 기기는 조용히 실패하고 호출부가 안내 문구.
- 탭이 백그라운드로 가면 자동 해제되므로 `visibilitychange`에서 재획득.

**적용**
- `Breathe.jsx`: `playBowl` → `playBreathTone`. 멈춤(hold)도 낮게 깔아 무음 구간을 없앴다.
- `yogaSound.js`: 기존 패드 음색은 그대로 두고 패너만 통과시켰다(회귀 위험 최소화).
- 두 화면 모두 눈감기 모드 + Wake Lock. 30초 뒤 `opacity: 0.07`,
  탭하면 3초 복귀. 완전히 0으로 안 두는 이유는 "앱이 죽었나" 하는 불안을 막기 위해서다.
- ⚠️ **펄스 호흡(카팔라바티·바스트리카)은 눈감기 모드 제외** — 라운드·횟수를 봐야 한다.

**검증(실측)**
- 눈감기: 30초 뒤 `is-dimmed` + `opacity 0.07` 확인. 탭 → false → 3초 뒤 다시 true 확인
- 펄스 호흡 준비 화면에 눈감기 토글 미노출 확인 / 일반 요가에는 노출 확인
- `wakeLock in navigator` = true (테스트 환경), 콘솔 에러 0, 빌드 성공

**남은 것**: 나레이션이 아직 영어 "inhale · hold · exhale"이다. 한국어 음성으로 바꾸면
연속음과 합쳐져 완성도가 올라간다(TTS 생성 또는 녹음 필요).

### 2026-08-19 — DONE (수행일지 3갈래 연동)
그동안 요가·관찰 수행은 마쳐도 기록이 남지 않았다. 홈에 목적별 진입로를 만들어
유입을 늘려놨는데 돌아올 이유가 없으면 새는 구조라, 이걸 먼저 막았다.

**DB (production 마이그레이션 적용됨)**
- `sessions`에 `track`(anapanasati|yoga|vipassana), `practice`(실습 id) 추가.
  추가 전용이라 기존 4개 행은 default로 'anapanasati'가 되어 그대로 유효. 실측 확인함.
- `sessions_user_track_created_idx` 인덱스 추가. `supabase/schema.sql`도 동기화.
- ⚠️ `stage`는 아나빠나사띠 전용이라 다른 갈래는 0으로 넣는다(store.js에서 처리).

**신규: `src/lib/tracks.js`**
- `describeSession(row)` → `{track, title, detail, backTo}`. 화면마다 if문으로 갈래를
  분기하면 금방 흩어지므로 "기록 한 줄 → 표시용 정보" 변환을 한 곳에 모았다.
  갈래가 늘어도 이 파일만 고치면 된다.

**연동**
- `YogaPractice.finish()` → `/complete`로. 30초 미만은 기록할 게 없어 상세로 돌려보낸다.
  ⚠️ pulsed 모드는 setTimeout 체인이라 초 카운터가 없어서, 두 모드 모두 `startedAtRef`
  (시작 시각) 기준으로 경과를 잰다.
- `VipassanaSession` → 마무리 화면에 **일지에 남기기** / 기록하지 않고 나가기.
  관찰 수행은 마친 여운이 중요해서 곧바로 기록으로 밀지 않고 선택하게 했다.
- `SessionComplete`·`Journal`·`JournalDetail`이 모두 `describeSession`을 쓰도록 교체.
  일지 목록·상세에 갈래 태그(🫧/🌀/💧) 표시. `.track-tag` 스타일 추가.

**검증**
- 관찰 수행 → "💧 관찰 수행 · 숨의 일어남과 사라짐 · 신념처" 표시 확인
- 요가 1분 → "🌀 요가 호흡 · 복식호흡 · Diaphragmatic" 표시 확인
- 요가 30초 미만 → 상세로 복귀 확인
- DB: 트랜잭션 롤백으로 insert 검증(track/practice/stage 정상 수용), 테스트 행 미잔류(총 4행 유지)
- 빌드 성공, 새 탭 콘솔 에러 0
  (기존 탭의 useAuth null 에러는 Vite HMR이 컨텍스트 모듈을 교체할 때 생기는 개발 전용 현상)

**남은 것**: 로그인 상태의 실제 저장은 대표님 계정으로 확인 필요(제가 로그인할 수 없음).

### 2026-08-09 — DONE (기본 호흡수 교정 · 프라나야마 안전 게이트)
> 근거: 호흡명상 UX 리서치. 5개 제안 중 1·2순위로 판단한 것을 먼저 적용.

**① 기본 호흡수가 근거 범위 밖이었다 (버그성)** — `src/pages/Breathe.jsx`
- 공명주파수 연구 합의는 **분당 4.5~6.5회**. 그런데 기본값 `4-4-6-2`는 16초 주기 = **분당 3.75회**로 범위 밖이었다. 입문자가 첫 화면에서 이걸 만나 억지로 따라가면 과호흡·어지럼이나 이완유발불안(RIA)으로 간다.
- 패턴 재구성: **기본 `4-0-6-0`(분당 6회)** · 고요 `6-0-6-0`(5회) · 심화 `4-4-6-2`(3.8회) · 사각 `4-4-4-4`(3.8회) · 수면 `4-7-8-0`(3.2회). 느린 패턴은 없애지 않고 이름만 옮겼다.
- **모든 칩에 분당 호흡수 병기** — 무엇을 고르는지 보이게.
- **세션 중 `조금 느리게`/`조금 빠르게`** (들숨·날숨에만 ±1초, 멈춤은 유지). localStorage에 저장돼 다음 세션에 이어짐. 개인 공명주파수는 재검사 시 3분의 2가 바뀌므로, 정밀 측정 기능보다 사용자가 그 자리에서 찾는 편이 근거상 합리적이다.
- ⚠️ **타이머 루프가 `activePhases`를 클로저로 잡고 있어** 세션 중 조절이 반영되지 않았다. `activePhasesRef`로 매 페이즈 경계마다 최신값을 읽게 수정.
- 준비 문구를 "잠시 후 시작합니다" → "편안히 앉으셨다면, 곧 시작합니다"로. "숫자를 정확히 맞추지 않아도 됩니다" 안내 추가.

**② 프라나야마 안전 게이트** — `src/data/pranayama.js`, `src/pages/YogaPractice.jsx`
- 기존에 `requireConsent`와 금기 목록은 있었으나 **뇌전증(과호흡이 발작 유발 가능)·역류성 식도염(복부 수축이 위산 역류 촉진)·공황장애가 빠져 있었다.** 추가.
- **초회 라운드 제한**: 카팔라바티·바스트리카는 첫 3회까지 `firstSessionRounds: 1`로 강제(지침은 초회 1~2분 이내). 완주 횟수는 localStorage에 기록되고, 준비 화면에 "(0/3회 완료)"로 안내.
- **세션 중 상시 `어지러워요 · 바로 멈추기` 버튼** → 즉시 중단 + 회복 안내(깊게 쉬지 말고 얕게 / 발바닥 감각으로 / 1~2분 내 가라앉음 / 지속되면 의료진).
- 동의 화면을 한 줄 나열에서 **금기·주의 목록**으로 확장.
- 카팔라바티는 **기흉 증례보고가 있는 기법**이라 이건 선택이 아니라 필수.

- 검증(실측): 기본 칩 "분당 6회" 표시 확인. `조금 느리게`×2 → 분당 6회→4.3회, 저장값 2. **러닝 중 페이즈 길이를 111회 샘플링해 들숨 6초·날숨 8초로 실제 반영 확인.** 카팔라바티 1라운드 제한·회복 화면 동작 확인. 콘솔 에러 0.
- **배포 완료** — `assets/index-2zbu0G5K.js`, `assets/index-BlY0QPGq.css`
- **미적용(리서치 제안 중)**: 청각 페이서 승격+공간음향+눈감기 모드(Wake Lock 필수 동반), 브레스 카운팅 실습, 마이크 날숨 감지.

### 2026-08-09 — DONE (관찰 수행/위빠사나 구현 · 배포)
> 기획: `docs/VIPASSANA_PLAN.md` · 언어 규칙: `docs/PRODUCT_STRATEGY.md` 2-3

**신규 파일 (충돌 없음)**
- `src/data/vipassana.js` — 개념·사념처 4·5단계·실습 6종. **`attitudes` 키는 전통이 아니라 태도**(observe/accept)로, MBSR을 붙일 때 `accept` 키만 추가하면 같은 실습을 재사용한다.
- `src/pages/Vipassana.jsx`(허브) / `VipassanaSatipatthana.jsx`(사념처) / `VipassanaDetail.jsx`(상세) / `VipassanaSession.jsx`(4모드 엔진) / `Vipassana.css`
- `src/lib/vipassanaSound.js` — 종(bell). 비정수배 배음으로 금속 울림 합성. `bowl.js`의 AudioContext 공유(bowl.js 미변경).

**핵심 설계 — 화면이 호흡을 지시하지 않는다**
요가처럼 "들이쉬기 4초" 오브를 쓰면 위빠사나를 호흡 조절법으로 오해한다. 오브는 호흡과 무관한 8초 자율 맥동만 하고, 사운드도 박자를 지시하지 않고 구간 전환만 표시한다. 준비 화면에도 "숨은 조절하지 마세요"를 명시.

**실습 4모드**
- `guided` — 구간별 안내문 전환. `script`의 `w`는 상대 가중치라 5/10/15분 어느 쪽을 골라도 같은 비율로 늘어난다.
- `scan` — 부위 18개를 위→아래 훑고 되돌아옴(35스텝). 인체 실루엣 SVG에 글로우가 내려간다.
- `walking` — 들림·나아감·놓음 3박자, 좌/우 발 교대. 발 자체가 움직여 박자를 보여준다.
- `open` — 대상 없음. 화면 거의 비우고 2/3/5분 간격 종소리만. 시작 전 동의 필요.

**2층 언어 적용**
메뉴·제목은 "관찰 수행", 전통 용어(위빠사나·신념처 등)는 eyebrow와 접어 둔 `<details>` "이 수행의 유래"에만.

**공용 파일(추가만)**
- `src/App.jsx` — 라우트 4개 추가
- `src/components/Navbar.jsx` — `PRACTICE_LINKS`에 "관찰 수행" 한 줄 추가 (드롭다운 덕분에 네비는 그대로 5개)

**구현 중 발견해 고친 버그 3건**
1. **세션이 끊기지 않던 문제** — `/vipassana/:id/practice`가 같은 라우트 패턴이라 실습을 바꿔도 컴포넌트가 재사용되면서 이전 타이머가 계속 돌고 선택값이 어긋났다. `id` 변경 시 초기화하는 effect 추가.
2. **시작 시 스크롤이 남던 문제** — `index.css`의 전역 `scroll-behavior: smooth` 때문에 `window.scrollTo(0,0)`가 완료되지 않았다. `behavior:'instant'`로 명시.
3. **실루엣이 배경에 묻힘** — `--bg-sunken` 채움이 `--bg`와 대비가 없었다. 테마 색조를 옅게 깐 rgba로 교체.

**함께 보완한 것**
- `.page`에 배경이 없어 장르 색이 카드에만 적용되던 문제 — `.vipassana-theme`·`.yoga-theme`에 `background: var(--bg)` 추가해 화면 전체에서 장르가 읽히게 함.
- 파문은 볼 것이 오브뿐인 모드(guided·open)에서만. 몸 훑기·경행에는 실루엣·발이 있어 겹치면 산만하다.

- 검증: 빌드 성공(125 모듈), 콘솔 에러 0, 4모드 실동작 확인, 360px 가로 넘침 없음. **배포 완료** — `assets/index-A5rPzESm.js`, `assets/index-DAXDVRlK.css`
- **범위 밖(다음)**: 실습 완료를 수행일지에 기록(요가도 미연동), 자애(사무량심), MBSR.

### 2026-08-07 — DONE (모바일 결함 2건 수정 · '수련' 드롭다운)
- **① '수련' 드롭다운 신설** — `src/components/Navbar.jsx` 개편, `src/components/Navbar.css` 신규.
  - 네비 **6개 → 5개** (배우기 · 수련▾ · 수행일지 · 프리미엄 · 로그인). `호흡하기`·`요가 호흡`을 드롭다운으로 묶음.
  - 부제는 1층 언어로 — 호흡하기 "숨을 지켜보기" / 요가 호흡 "숨을 다스리기".
  - 터치 대응: hover가 아니라 **클릭 토글**. 바깥 `pointerdown`·ESC로 닫고, 라우트 변경 시 자동으로 닫는다.
  - 접근성: `aria-haspopup="menu"`, `aria-expanded`, `role="menu"/"menuitem"`. 하위 경로에 있으면 `수련` 자체가 active.
  - `transform`이 위치 지정(가운데 정렬)에 쓰이므로 애니메이션은 **opacity만** 다룬다. 380px 이하에서는 `left:0`으로 전환해 잘림 방지.
  - **→ 위빠사나·자애를 넣을 자리가 확보됨.** `PRACTICE_LINKS` 배열에 한 줄 추가하면 된다.
- **② 요가 아쉬탕가 카드 모바일 깨짐 수정** — `src/pages/Yoga.css`. 640px 이하에서 `flex-direction: column` + 화살표를 `align-self: flex-end`로. 세로 가운데 정렬된 화살표가 본문 중간에 끼던 문제 해결.
- 검증 (실측):
  - 360px: 링크 5개, 마지막 링크 우측 336 < 컨테이너 360 → **잘림 해소**(이전엔 383 > 375로 잘림). 가로 넘침 없음.
  - 1280px: 잘림 없음.
  - 드롭다운 토글 정상(`aria-expanded` true/false), 360·375px 모두 뷰포트 안, `z-index:60`으로 네비(50) 위, 배경 불투명(`rgb(251,250,247)`), 중앙 히트테스트가 메뉴 항목을 반환 → 클릭 가능.
  - 현재 경로가 `/yoga`일 때 `요가 호흡` 항목이 `is-current`로 강조됨.
- 빌드 성공 → **배포 완료**. 에셋: `assets/index-BdfaAALq.js`, `assets/index-BBfK3Fj9.css`

### 2026-08-07 — DONE (분담 해제 · 배포)
- **Codex·Claude 분담 종료.** 원래 토큰 사용량 때문에 일시적으로 나눈 것이었음. 이제 **결제·인증·배포를 포함한 전 영역을 Claude가 담당**한다. `HANDOFF.md` 0-1절 개정. `docs/HANDOFF_CODEX.md`는 이력 참고용으로만 보존.
- **배포 완료** — `npm run build`(117 모듈) → `npm run deploy` → `Published`.
  - **요가 호흡법이 처음으로 라이브 공개됨** (그동안 READY_FOR_DEPLOY 상태였음).
  - 빠른 개선 3건(무료 시간 해제 · 16일 여정 진도바 · 네비 팔리어 제거)도 함께 반영.
  - 배포 에셋: `assets/index-pOoBCFB6.js`, `assets/index-CIPG9DuC.css`
- **캐시 주의**: 배포 직후 브라우저에 이전 화면이 남았다. 서버 HTML은 새 에셋을 가리키고 있었으므로 전파 지연이 아니라 **브라우저 캐시**였다. 검증할 때는 쿼리스트링 등으로 캐시를 우회할 것.
- 검증: 라이브 URL에서 데스크톱·모바일(375px) 확인, 콘솔 에러 0, 가로 넘침 없음.
- **라이브에서 발견한 모바일 결함 2건 (미수정, 다음 작업 후보):**
  1. **네비 마지막 링크 '로그인'이 화면 오른쪽에서 잘린다** (383px 측정: 링크 우측 383 > 컨테이너 375). 링크 6개가 이미 한계. → `VIPASSANA_PLAN.md`의 '수련' 드롭다운이 선행 조건임이 실측으로 확인됨.
  2. **요가 아쉬탕가 카드의 화살표(→)가 모바일에서 본문 중간에 끼어든다.** `.yoga-ashtanga-card`가 flex row 고정이라 좁은 폭에서 텍스트가 화살표를 감싸며 깨져 보인다. 모바일에서 column 전환 필요.

### 2026-08-07 — DONE (빠른 개선 3건 · 전략 문서 반영)
> 근거: `docs/PRODUCT_STRATEGY.md` 10장 "지금 바로" 항목
- **① 무료 수행 시간 제한 해제** — `src/pages/Breathe.jsx`. `DURATIONS`를 3/5/10분(10분 프리미엄) → **3/5/10/15/20분 전부 무료**로. 5분은 제1념처도 못 끝내는 길이라 핵심 가치를 체험시키지 못했다. 프리미엄은 시간이 아니라 편의(가이드음성·커스텀패턴·오프라인)로 판다.
- **② 16단계 → "16일 여정" + 진도바** — `src/pages/Learn.jsx` 개편, `src/pages/Learn.css` 신규.
  - `sessions.stage`로 이미 걸어본 단계를 집계 → 진도바 + 단계 카드 ✓ 표시.
  - 헤딩을 "아나빠나사띠 열여섯 걸음" → **"하루에 한 걸음씩, 열여섯 걸음"**(1층 언어). 경전 출처는 하단 각주(2층)에 유지.
  - 문구는 리셋·처벌 없는 톤 — "건너뛴 걸음이 있어도 괜찮습니다. 오늘 앉는 것이 여정입니다."
  - 비로그인 시 진도바 대신 로그인 안내. 조회 실패해도 학습 콘텐츠는 그대로 렌더(catch 무시).
- **③ 네비 팔리어 병기 제거** — `src/components/Navbar.jsx`. `Ānāpānasati` span 삭제(2층 규칙).
- 검증: 빌드 성공(117 모듈). 콘솔 에러 0. 데스크톱·모바일(375px) 스크린샷 확인, 가로 넘침 없음. 진도바는 로그인 상태를 만들 수 없어 **DOM 주입으로 렌더링만 검증**(로직은 실제 로그인 후 확인 필요).
- 공용 파일: `src/components/Navbar.jsx`만. 작업 시점 워킹트리에 Codex 변경 없었음(충돌 없음). 결제·인증 미변경.
- **발견한 기존 이슈(미수정):**
  - 383px에서 네비 마지막 링크(로그인)가 컨테이너 우측 패딩을 넘어 화면 끝에 붙는다. 기능 문제는 아니나 **7번째 항목(위빠사나)을 넣을 여유가 없음을 확인** → `VIPASSANA_PLAN.md`의 '수련' 드롭다운이 선행 조건.
  - `index.html`의 `<title>`이 여전히 "숨결의 길 · Ānāpānasati Path" — 1층 규칙 위반. Codex가 no-cache 메타를 넣은 파일이라 임의 수정하지 않음.
  - 호흡 패턴 잠금(이완 4-7-8 / 사각 4-4-4-4)은 승인 범위 밖이라 유지. 전략 문서상 프리셋은 무료, 프리미엄은 "커스텀 패턴"이 맞아 재검토 필요.
  - 일지 7일 열람 제한(`src/pages/Journal.jsx`)도 승인 범위 밖이라 유지.

### 2026-08-07 — PLAN (시장 조사 + 아키텍처 v2 + 위빠사나 기획)
- 요청: 위빠사나 기능 구현 → 논의 중 MBSR 추가 계획이 나와 아키텍처 재설계로 확장 → 경쟁 앱 조사 → 기획 문서 정리.
- **시장 조사 3건 수행** (글로벌 앱 / 국내 시장 / 기능·수익화 벤치마크). 원본: `docs/global-meditation-apps-research-2026.md`.
- **핵심 발견 (전략 변경 근거):**
  - 국내 전업 명상앱 2025 합산 매출 **7.5억** (마보 4.39억·영업적자 / 마음수업 3.15억·5년 연속 적자). 시장조사기관 TAM 2,700억과 3자릿수 괴리.
  - B2C 구독의 끝은 이미 두 번 증명됨 — 코끼리(2023)·트로스트(2025) 피인수. 돈은 B2B에 있음(아토머스 96.6억 vs 마음수업 3.15억).
  - 글로벌도 역성장 — Calm 매출 -24%, Headspace MAU 정점 대비 -64%·인력 13% 감원.
  - **불교 정면 노출은 리스크** — 육도윤회명상 100+ vs 마보 10만+. 마보는 "종교색을 덜어냈다"가 공식 포지셔닝. 글로벌·국내 조사가 독립적으로 **"바깥은 세속어, 안은 전통어"** 2층 구조로 수렴.
  - ⚠️ **초기 판단 정정**: 불교 호감도 51.3점을 근거로 "정면 노출 = 기회"라 판단했으나 실측 데이터로 뒤집힘.
- **설계 결정:**
  - `features/` 단일 축 → **대상 × 태도 5축**(조절·집중·관찰·계발·수용) + `core/practices/traditions/paths/goals` 레이어. MBSR을 형제 feature로 두면 바디스캔·경행·열린알아차림을 3중 구현하게 되는 범주 오류를 회피.
  - framing 키를 **전통이 아니라 태도**로. → MBSR 추가 = 파일 하나.
  - 구성 로드맵: **위빠사나 → 자애(사무량심) → MBSR**. 사마타는 아나빠나사띠와 겹쳐 보류.
  - 위빠사나 실습 4모드 신설(guided·scan·walking·open). **오브가 호흡을 지시하지 않는다**(조절법 오해 방지).
- 수정 파일: `docs/ARCHITECTURE.md`(v2 개정), `docs/PRODUCT_STRATEGY.md`(신규), `docs/VIPASSANA_PLAN.md`(신규), `docs/HANDOFF_CLAUDE.md`.
- 검증: 문서 기획만. **코드 변경 없음.**
- Codex와 겹치는 파일: 없음. **단, Codex에 전달할 사항 있음** ↓
- **→ Codex 전달 필요 (결제·프리미엄 소유 영역):**
  1. **가격 개정** — 정가 월 9,800→**6,900**, 연 98,000→**49,000**. 오픈기념가(4,900/39,000) 유지. 3개월 구간 12,900 신설. (근거: 마보 5,900/47,000, 코끼리 6,900/45,000)
  2. **무료 경계선 이동** — 수행 5분 제한 해제, 일지 7일 제한 해제, 연속기록·통계 무료화. 프리미엄은 가이드음성·AI코칭·커스텀패턴·오프라인·배경음으로. (근거: D30 리텐션 Insight Timer 16% vs Headspace 4.7%)
  3. **2025.2.14 한국 구독 규제 준수** — 다크패턴 6종 금지, 위반 시 최대 500만원. **취소 경로가 구매 경로보다 번거로우면 안 됨.**
  4. 선물하기(기프트) 결제 — 마보·코끼리 모두 운영. 요가원·소울라이즈 커뮤니티에 적합.
- 남은 일: 이관 범위 확정(엔진 추출 선행 여부) → 위빠사나 구현. 브랜드 표면층 수정(네비 `Ānāpānasati` 병기 제거 등)은 별건으로 처리 가능.

### 2026-08-03 — PLAN (요가 호흡법 기획)
- 요청: 요가 호흡법 기능 추가. 전체 5단계 기법 다 + 학습목록/상세 + 호흡 타이머. **아쉬탕가 8단계 학습 필수**. 초보자 친화(개념 쉽게 + 바로 실습).
- 설계/콘텐츠 결정: `docs/PRANAYAMA_PLAN.md` 작성 — 개념(프라나야마 vs 아나빠나사띠), 아쉬탕가 8단계, 5단계 기법(복식·완전요가·박스·나디쇼다나·웃자이·브라마리·시탈리·카팔라바티·바스트리카), 타이머 3모드(paced/alternate/pulsed), 화면·데이터 구조.
- 수정 파일: `docs/PRANAYAMA_PLAN.md`(신규), `docs/HANDOFF_CLAUDE.md`.
- 검증: 문서 기획만, 코드 변경 없음.
- Codex와 겹치는 파일: 없음. (구현 시 `src/App.jsx`, `src/components/Navbar.jsx` 수정 예정 → 착수 전 이 로그에 IN_PROGRESS 기록 후 병합)
- 남은 일: 데이터(`ashtanga.js`,`pranayama.js`) → 페이지 → 타이머 → 라우트·네비 순으로 구현. 사용자 승인 후 착수.

## 작업 기록 형식

```md
### YYYY-MM-DD HH:mm — STATUS
- 요청:
- 설계/콘텐츠 결정:
- 수정 파일:
- 검증:
- Codex와 겹치는 파일:
- 남은 일:
```

상태 값: `IN_PROGRESS`, `READY_FOR_REVIEW`, `READY_FOR_DEPLOY`, `DEPLOYED`, `BLOCKED`.
